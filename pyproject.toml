[project]
name = "agentic-ai-benchmarking-framework"
version = "0.1.0"
description = "Agentic AI benchmark framework scaffold for PCIe GPU servers."
requires-python = ">=3.10"
dependencies = []

[project.scripts]
aab-framework = "aab_framework.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
